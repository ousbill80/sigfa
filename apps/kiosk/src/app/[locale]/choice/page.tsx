/**
 * MODEL-KIOSK-B — app/[locale]/choice/page.tsx
 * Point d'entrée « Que souhaitez-vous ? » : DEUX chemins (opération / conseiller).
 */
import { ChoiceScreen } from "@/components/ChoiceScreen";

export default function ChoicePage() {
  return <ChoiceScreen />;
}
