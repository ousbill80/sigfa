/**
 * KIOSK-002 — app/[locale]/page.tsx
 * Page d'accueil kiosque : sélection de la langue.
 */
import { HomeScreen } from "@/components/HomeScreen";

export default function HomePage() {
  return <HomeScreen />;
}
