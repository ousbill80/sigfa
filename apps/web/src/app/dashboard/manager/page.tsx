/**
 * Manager dashboard page.
 * @module app/dashboard/manager/page
 */
import type { ReactElement } from "react";

export default function ManagerDashboardPage(): ReactElement {
  return (
    <main role="main" style={{ padding: "2rem", backgroundColor: "var(--surface-0)" }}>
      <h1 style={{ color: "var(--ink-strong)" }}>Tableau de bord Manager</h1>
    </main>
  );
}
