# RT-003 : E2E Playwright — parcours complets bout-en-bout avec coupure réseau

**Module** : RT · **Agent** : direct (orchestrateur pilote ; pas de couche métier nouvelle) · **Dépend de** : RT-002 · **Statut** : TODO

**Révision** : **v2 — arbitrage `docs/sessions/2026-07-12/31-critique-arbitrage-rt.md`** (D9 : repli Electron par défaut + seuil objectif)

> Résorbe le repli « Electron E2E complet = RT-003 » consigné en F4 (KIOSK-001). Exerce le produit réel de bout en bout, plusieurs surfaces à la fois, sur l'API + sockets réels.

## Exigences (EARS)
- **UBIQUITAIRE** : un scénario Playwright orchestre le **parcours complet réel** : (1) un client prend un ticket à la **borne**, (2) l'**écran TV** affiche l'appel (`ticket:called`) quand un **agent** appelle le suivant depuis son dashboard, (3) l'agent **sert puis clôture**, (4) le client **laisse un feedback** (suivi public par trackingId) — le tout contre l'API réelle + sockets réels (RT-001), pas le mock.
- **QUAND** une **coupure réseau est simulée mi-parcours** (offline navigateur), **ALORS** les surfaces dégradent proprement (états offline F4) puis, au retour, **resynchronisent** (`sync:state`) sans perte du ticket en cours — assertion sur l'écran final cohérent.
- **UBIQUITAIRE** : le filet automatisé de la borne = **E2E navigateur (Next hors Electron)**. La **borne Electron réelle** en CI (xvfb + libgbm documenté) est best-effort ; le **repli gate humain démo tracé** n'est autorisé qu'**APRÈS ≥2 runs CI Electron rouges consignés** (seuil objectif, jamais auto-attestant), trace = fichier `docs/sessions/…` daté et lié à la démo.
- **anormal** : un parcours qui échoue laisse des artefacts exploitables (trace Playwright, captures) ; aucun test « vert vide ».

## Critères d'acceptation
- [ ] `RT-003: parcours borne→appel TV→service agent→feedback vert de bout en bout contre l'API + sockets réels`
- [ ] `RT-003: coupure réseau mi-parcours → dégradation offline puis resync sans perte, écran final cohérent`
- [ ] `RT-003: E2E navigateur automatisé (filet) vert ; borne Electron réelle best-effort (xvfb/libgbm) ou repli tracé APRÈS ≥2 runs CI rouges consignés`
- [ ] `RT-003: artefacts (traces/captures) produits ; CI verte`

## Hors scope
Charge/perf (SEC-004) · parcours dépendant d'écrans F4 non construits (marqués PENDING F4) · notifications réelles/OTP (F6) · tests visuels externes (Playwright natif suffit).
