# API-006 : Socket.io serveur conforme au contrat + verrou d'appel

**Module** : F3 — API · **Agent** : agent-api · **Dépend de** : API-003 · **Statut** : DONE (2026-07-12)

> **DONE** : serveur Socket.io attaché à l'HTTP Hono (harness), handshake JWT avant join, rooms `agency:{id}` scopées, adapter Redis pub/sub, émetteur typé `emitTicketCalled` (payload invalide → non émis + log), verrou durci Redis SET NX PX 5000 + re-vérification transactionnelle FOR UPDATE (course 20 paires : zéro double-attribution ; call ciblé perdant → 409 `TICKET_ALREADY_CLAIMED`), `sync:request`→`sync:state` (recentCalls CONTRACT-012), suite `realtime-guarantees` (p95 <500 ms / 50 émissions). **146 tests verts**, `socket-server.ts` couverture 100/83.33/100. Commit `520e00c`.
> **Différé RT-001** (branchement réel) : câblage `index.ts` prod (`serve()`→`createSocketServer`) + adossement `RealtimeBus`↔`io` — voir note frontière du `_dag.md`.

**Révision** : v2 — arbitrage 19

## Exigences (EARS)
- **Wiring** : `const server = serve(honoApp)` (@hono/node-server) puis `new Server(server)` — HTTP et upgrade WebSocket coexistent sur le **même port** ; le handshake JWT est authentifié **AVANT** le join de room.
- Serveur Socket.io 4 attaché à l'API : handshake authentifié JWT (utilisateur OU session borne) ; un socket ne peut joindre QUE `agency:{id}` de son scope (tentative hors scope → refus + log).
- CHAQUE événement émis passe par un émetteur typé qui valide le payload contre `payloadSchema` Zod du contrat (realtime.ts) — payload invalide : événement NON émis + erreur loggée (jamais de payload sauvage).
- Adapter **Redis pub/sub** (multi-instance ready) ; émissions branchées sur API-003/004/005 (created/called/closed/queue:updated/counter:status).
- **Verrou d'appel durci** : QUAND 2 agents font call-next/call simultanément, UN SEUL obtient le ticket (Redis SET NX PX 5000 par ticket + re-vérification transactionnelle du statut) ; le perdant de call-next reçoit LE SUIVANT (pas une erreur) ; le perdant de call ciblé reçoit 409 `TICKET_ALREADY_CLAIMED`. Test de course RÉEL (≥20 paires concurrentes, zéro double-attribution).
- **Resync** : `sync:request` → `sync:state` (état complet de la file de l'agence : tickets WAITING/CALLED, compteurs, guichets) — contrat 002 ; reconnexion testée.
- Suite `realtime-guarantees` branchée : `ticket:called` reçu par un client local en <500 ms (TICKET_CALLED_SLA_MS) mesuré sur 50 émissions, p95.

## Critères d'acceptation
- [ ] `API-006: handshake sans JWT → refus ; join room hors scope → refus (tests)`
- [ ] `API-006: payload non conforme → événement bloqué + log (test avec schéma violé)`
- [ ] `API-006: course 20 paires call-next → zéro double-attribution, chaque perdant obtient le suivant (test de course réel)`
- [ ] `API-006: call ciblé concurrent → un 200, un 409 TICKET_ALREADY_CLAIMED`
- [ ] `API-006: sync:request → état complet exact après reconnexion (test)`
- [ ] `API-006: wiring — route HTTP et upgrade WS coexistent sur le même port ; handshake JWT authentifié AVANT join (test Supertest + socket.io-client)`
- [ ] `API-006: ticket:called p95 <500ms sur 50 émissions locales (suite realtime-guarantees)`

## Hors scope
Clients socket (F4/RT-001) · charge 50 agences (SEC-004) · alert:manager (API-007) · kiosk:printer-error (API-011 l'émet depuis le heartbeat).
