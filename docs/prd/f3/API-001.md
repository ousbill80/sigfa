# API-001 : Auth — login, refresh rotation, logout, me, blocage

**Module** : F3 — API · **Agent** : agent-api · **Dépend de** : F2 DONE · **Statut** : TODO

**Révision** : v2 — arbitrage 19

## Exigences (EARS)
- **Périmètre confirmé** : le bootstrap Hono/Pino/Redis/dépendances (liste explicitée ci-dessous) EST le périmètre d'API-001 — aucune story antérieure « API-000 » n'est créée (rejet entériné). Dépendances explicites : `hono`, `@hono/node-server`, `pino`, `ioredis`, `drizzle-orm`, `@sigfa/database`, `@sigfa/schemas`, `@sigfa/contracts`, `zod`, `jose`, `bcrypt`, `nanoid`, `bullmq`.
- Le squelette Hono (`apps/api/src/app.ts` + `index.ts`) doit exposer `/api/v1` avec gestion d'erreur globale au format LA LOI et logs **Pino** structurés (remplace le placeholder F0 — console.log interdit désormais).
- `POST /auth/login` : QUAND email+password valides, retourner access JWT **15 min** (claims : sub, bankId, role, agencyIds) + refresh token **7 j** opaque stocké Redis (`refresh:{jti}` → userId, rotation) ; les hash bcrypt viennent de `users` via la connexion applicative SANS contexte tenant pour la résolution du login (email unique global — requête dédiée documentée, la seule hors withTenant avec les routes platform).
- SI 5 échecs en 15 min pour un email (compteur `users.failed_login_attempts` + `locked_until`), ALORS 429 conforme LA LOI ; QUAND `locked_until` est dépassé, le compteur se réinitialise au succès.
- `POST /auth/refresh` : rotation stricte — l'ancien refresh est invalidé atomiquement (Redis GETDEL) ; SI un refresh déjà consommé est rejoué, ALORS 401 + révocation de TOUTE la famille de tokens (détection de vol, documentée).
- `POST /auth/logout` : `security: []` (LA LOI) — révoque le refresh fourni ; idempotent. `GET /auth/me` : profil du JWT (AUTHENTICATED).
- SI `JWT_SECRET` absent/court (<32 chars), ALORS échec au démarrage (fail-fast, pattern DB-008). `.env.example` enrichi.
- Utilisateur `deleted_at`/`is_active=false` → 401 au login ET au refresh.

## Critères d'acceptation
- [ ] `API-001: login valide → access décodable (claims exacts) + refresh ; mauvais mdp → 401 code LA LOI`
- [ ] `API-001: 5 échecs/15min → 429 ; après expiration du verrou → login OK et compteur remis (horloge contrôlée)`
- [ ] `API-001: refresh → nouveaux tokens, l'ancien rejoué → 401 + famille révoquée (test de rejeu)`
- [ ] `API-001: logout sans Authorization valide fonctionne (security:[]) ; me → 401 sans token, profil avec`
- [ ] `API-001: utilisateur inactif/supprimé → 401 login et refresh`
- [ ] `API-001: JWT_SECRET manquant → démarrage échoue explicitement`
- [ ] `API-001: Schemathesis PASS sur le module auth contre l'API réelle (PG+Redis Testcontainers)`

## Hors scope
Middleware tenant/RBAC des autres routes (API-002) · session borne (API-009) · rate limiting IP global (API-011) · UI login (WEB-001).
