# API-007 : Agents — statuts temps réel, chronomètre, alertes manager

**Module** : F3 — API · **Agent** : agent-api · **Dépend de** : API-004, API-006 · **Statut** : DONE (2026-07-12)

> **DONE** : `POST /agents/:id/status` (machine à états AVAILABLE/SERVING/PAUSED/ABSENT/OFFLINE, 409 sur transition illégale, `agent_status_history` + `counter:status`), SERVING piloté par le cycle ticket (forçage manuel → 409), `GET /agents/:id/stats` (self AGENT / scope MANAGER, RBAC), anti-flap par `agentId` (Redis SET NX TTL 30 s, reconnexion → DEL), AGENT_DISCONNECTED_WITH_TICKET → WAITING PRIORITY + alerte, jobs BullMQ repeatable `inactive-agent-scan`/`sla-scan` à verrou distribué (concurrency=1 ; course 2 instances → UNE alerte), SLA_BREACH + `escalated` >2×, `config/alerting.ts` intervalles injectables env. Schemathesis module agents PASS. **240 tests verts**, couverture nouveaux fichiers ≥94% stmts/90.78% branch. Commit `2b86d26`.
> **Dette API-005 RÉSORBÉE** : `alertManagerSchema` local unifié sur la forme contractuelle unique `{type, payload}` (union + `alertManagerOverflowSchema` supprimés) ; émission `QUEUE_CRITICAL` d'API-004 migrée en `{type:"QUEUE_CRITICAL", payload:{…}}`. `counter:status` (OPEN/PAUSED/CLOSED) ajouté au bus.
> **Différé RT-001** (branchement réel) : appel de `startAlertScheduler` (BullMQ) + câblage bus/socket au bootstrap `index.ts` — cohérent avec la frontière RT-001 déjà consignée (API-006).

**Révision** : v2 — arbitrage 19

## Exigences (EARS)
- `POST /agents/:id/status` : machine à états AVAILABLE/SERVING/PAUSED/ABSENT/OFFLINE (LA LOI agents.yaml, 409 dédié sur transition illégale — dont SERVING→ABSENT avec ticket ouvert sans transfert) ; chaque changement écrit `agent_status_history` + émet `counter:status`.
- SERVING est PILOTÉ par le cycle ticket (call→SERVING à serve, →AVAILABLE à close/no-show) — le poste ne force pas SERVING manuellement (409).
- `GET /agents/:id/stats` : tickets traités jour, TMT moyen jour, ticket en cours (numéro + durée chronométrée) — règle « self » : un AGENT ne lit que les siennes, MANAGER+ celles de son scope.
- **Anti-flap par agentId** : à la déconnexion socket, Redis `SET NX TTL 30 s` par `agentId` — reconnexion dans la fenêtre → `DEL` (aucun effet). Au-delà : traitement AGENT_DISCONNECTED_WITH_TICKET.
- **Alertes** (`alert:manager`, types LA LOI, room de l'agence, + persistées ? NON — instantanées, l'agrégat = F10 ; documenté) :
  - AGENT_INACTIVE : AVAILABLE sans appel depuis `agent_inactivity_minutes` (banks) — vérification par **job BullMQ repeatable** `inactive-agent-scan` (verrou distribué, `worker concurrency=1`) ; intervalle exporté depuis `config/alerting.ts`, injectable via env. UNE alerte par épisode.
  - SLA_BREACH : ticket SERVING dépassant le SLA du service — vérification par **job BullMQ repeatable** `sla-scan` (même verrou distribué) ; >2× SLA → alerte renouvelée marquée `escalated`.
  - AGENT_DISCONNECTED_WITH_TICKET : QUAND le socket d'un agent avec ticket CALLED/SERVING se déconnecte >30 s (grâce anti-flap par agentId), ALORS le ticket repasse **WAITING priorité PRIORITY** (v5), `counter:status` OFFLINE, alerte immédiate.
- Déconnexion socket SANS ticket → statut OFFLINE simple après la même grâce anti-flap.

## Critères d'acceptation
- [ ] `API-007: matrice de transitions statut exhaustive (légales/illégales → 409)`
- [ ] `API-007: serve/close pilotent SERVING/AVAILABLE ; forçage manuel SERVING → 409`
- [ ] `API-007: stats self-only pour AGENT, scope pour MANAGER (tests RBAC)`
- [ ] `API-007: inactif > seuil → UNE alerte ; activité → reset d'épisode (fake timers)`
- [ ] `API-007: SLA dépassé → alerte ; >2× → escalated (fake timers)`
- [ ] `API-007: anti-flap agentId — Redis SET NX TTL 30 s ; reconnexion <30 s → DEL, aucun effet ; >30 s → WAITING PRIORITY + alerte + history (Testcontainers Redis)`
- [ ] `API-007: jobs BullMQ repeatable inactive-agent-scan + sla-scan — verrou distribué concurrency=1 ; 2 instances → UNE seule alerte émise (test de course Testcontainers)`
- [ ] `API-007: intervalle alerting exporté config/alerting.ts, injectable via env (AGENT_INACTIVE_SCAN_INTERVAL_S…)`
- [ ] `API-007: Schemathesis PASS module agents (hors import CSV — API-009)`

## Hors scope
Import CSV + profils/compétences CRUD (API-009 pour import ; PATCH profil = API-008 périmètre admin) · notifications email/SMS des alertes (F6) · anomalies agrégées (F10).
