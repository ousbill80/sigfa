# API-008 : CRUD admin — banques, agences, services, guichets, files, horaires, RBAC, audit branché

**Module** : F3 — API · **Agent** : agent-api · **Dépend de** : API-002 · **Statut** : TODO

## Exigences (EARS)
- Implémenter LA LOI core+admin (hors périmètres 009/011) : Banks (GET/POST platform, GET/PATCH bank), Agencies (CRUD + DELETE soft avec 409 `AGENCY_HAS_OPEN_TICKETS`), Services (CRUD, code unique/agence, display_order), Counters (+ counter_services), Queues (PATCH open/pause), `GET/PATCH /agencies/:id/hours` (weekly + exceptional_closures + fériés en lecture), `GET/PATCH /banks/:id/thresholds`, `GET/PATCH /banks/:id/sms-templates` (validation variables → 422 `UNKNOWN_TEMPLATE_VARIABLE`), profil agent `GET/PATCH /agents/:id` (services, langues, work_schedule).
- CHAQUE mutation de ce périmètre écrit `insertAuditEntry` (qui/quoi/IP/diff — SEC-001 anticipé pour ce périmètre) — testé.
- `additionalProperties: false` du contrat = 422 réel sur champ inconnu (validation stricte).
- PATCH partiels : sémantique merge documentée par ressource (jamais d'écrasement silencieux d'hours par thresholds etc.).
- RBAC conforme matrice (BANK_ADMIN crée agences, DIRECTOR configure services/guichets de SON agence…) — hérite du middleware API-002, cas limites testés (DIRECTOR agence A sur agence B → 403).

## Critères d'acceptation
- [ ] `API-008: CRUD complet conforme LA LOI — Schemathesis PASS core+admin (hors 009/011)`
- [ ] `API-008: DELETE agence avec ticket ouvert → 409 ; sans → soft delete + invisible des listes`
- [ ] `API-008: champ inconnu → 422 ; variable template inconnue → 422 UNKNOWN_TEMPLATE_VARIABLE`
- [ ] `API-008: chaque mutation → entrée audit avec diff (test par ressource)`
- [ ] `API-008: RBAC cas limites — DIRECTOR hors scope agence → 403 ; AUDITOR lecture seule partout (tests)`
- [ ] `API-008: fermeture exceptionnelle n'écrase pas l'hebdo (test PATCH hours)`

## Hors scope
Theming/logo R2, clone, kiosk-access, purge-phone, audit-logs lecture (API-009/011) · reporting (F7) · notifications réelles (F6).
