# API-010 : Feedback client public — note, fenêtres, NPS

**Module** : F3 — API · **Agent** : agent-api · **Dépend de** : API-003 · **Statut** : DONE (2026-07-12)

> **DONE** : `POST /public/tickets/:trackingId/feedback` (note 1–5 + commentaire ≤500 sanitizé, fenêtre 24 h UTC strict `NOW()-closed_at > INTERVAL '24 hours'`, codes `TICKET_NOT_CLOSED`/`FEEDBACK_ALREADY_SUBMITTED`/`FEEDBACK_WINDOW_EXPIRED`, 404 opaque anti-énumération) ; `GET /public/tickets/:trackingId` (DTO public sans uuid, `Cache-Control: max-age=30` + ETag) ; agrégation NPS `daily_agency_stats` (5→promoter/4→passive/≤3→detractor, upsert idempotent par ticket via UPDATE conditionnel atomique). **Helper rate-limit Redis sliding-window réutilisable** `lib/rate-limit.ts` (feedback 5/min IP+trackingId, suivi 30/min, 429 + Retry-After) — **conçu pour montage global par API-011**. Schemathesis public PASS. **389 tests verts**, gate sérialisé 17/17. Commit `9b275d9` (main).
> **Note frontière** : borne 24 h stricte — à exactement T+24h00 `> 24h` est faux → autorisé (conforme à la formule LA LOI) ; expiré dès T+24h00+ε. Aucune couture base (colonnes feedback + compteurs NPS déjà présents F2).

**Révision** : v2 — arbitrage 19

## Exigences (EARS)
- `POST /public/tickets/:trackingId/feedback` (public, sans JWT) : note 1–5 entière + commentaire ≤500 ; ticket non DONE → 422 `TICKET_NOT_CLOSED` ; déjà noté → 409 `FEEDBACK_ALREADY_SUBMITTED` ; >24 h après close → 422 `FEEDBACK_WINDOW_EXPIRED` — fenêtre calculée en **UTC strict** : `NOW() - closed_at > INTERVAL '24 hours'` (jamais de décalage timezone) ; trackingId inconnu → 404 opaque (identique au format inconnu — pas d'oracle d'énumération).
- `GET /public/tickets/:trackingId` : statut/position/estimation publics (schéma LA LOI SANS uuid interne) + `Cache-Control: max-age=30` + ETag ; 404 opaque.
- **Anti-spam** : rate limit dédié par IP ET par trackingId (5 req/min feedback, 30 req/min suivi — Redis, 429 LA LOI) ; commentaire : strip HTML, longueur, rejet caractères de contrôle.
- Agrégation **NPS jour** : le feedback met à jour les compteurs `daily_agency_stats` (promoters 5, passives 4, detractors ≤3 — mapping documenté ; upsert incrémental idempotent par ticket).
- Aucune PII dans les réponses publiques ni les logs.

## Critères d'acceptation
- [ ] `API-010: fenêtre 24 h UTC strict — NOW() - closed_at > INTERVAL '24 hours' ; test horloge à T+23h59 → autorisé ; T+24h00 → 422 FEEDBACK_WINDOW_EXPIRED`
- [ ] `API-010: happy path + les 3 codes d'erreur de fenêtre/doublon (horloge contrôlée)`
- [ ] `API-010: trackingId inconnu ET malformé → 404 indistinguables (test anti-énumération)`
- [ ] `API-010: 6e feedback/min même IP → 429 ; suivi caché 30s (headers vérifiés)`
- [ ] `API-010: commentaire avec HTML/contrôles → nettoyé/rejeté (tests)`
- [ ] `API-010: NPS — 5→promoter, 4→passive, 3→detractor incrémentés une seule fois par ticket (rejeu idempotent)`
- [ ] `API-010: Schemathesis PASS module public (feedback+suivi)`

## Hors scope
Borne feedback UI (KIOSK-009) · insights NLP (F10) · notification de remerciement (F6).
