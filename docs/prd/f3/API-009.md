# API-009 : Onboarding & plateforme — clonage, accès borne, import CSV, theming, droit à l'oubli

**Module** : F3 — API · **Agent** : agent-api · **Dépend de** : API-008 · **Statut** : DONE (2026-07-12)

> **DONE** : clone-from (CONFIG seule, zéro donnée), session borne (`kiosk-access` credentials bcrypt one-shot + QR ; `POST /kiosk/session` → JWT 12 h ; `DELETE` → révocation vérifiée en middleware, 401 immédiat malgré `exp` valide), import CSV (≤500, E.164 → `INVALID_PHONE_FORMAT`, transaction par ligne, mots de passe jamais renvoyés), theming (WCAG luminance relative → `appliedColors` ≥4.5:1 mesuré ; presign R2 SigV4 local, sans config → 503 `R2_NOT_CONFIGURED`), purge-phone idempotent (`{purged:true}`→`{purged:false}` + audit `DATA_PURGE`) + retention-policy. Schemathesis admin PASS. **355 tests verts**, gate sérialisé 17/17. Commit `f5f6e14` (main).
> **Coutures consignées** : (1) `middleware/tenant.ts` gagne `assertKioskSessionActive` (révocation borne). (2) sessionId borne = `randomUUID()` (colonne `current_session_id` uuid). (3) le harnais de test `admin-test-harness.ts` re-déclare kiosks/retention_policies/notification_consents/enum printer_status/colonnes tickets — **vérifié : tous existent dans le schéma Drizzle réel** (kiosks.ts, retention.ts, notifications.ts, enums.ts, tickets.ts l.81/83/93/122) → aucune divergence source-de-vérité. (4) heartbeat `POST /kiosks/:id/heartbeat` minimal (usage JWT + révocation) ; supervision lecture = API-011.

## Exigences (EARS)
- `POST /agencies/:id/clone-from/:templateId` : copie CONFIG uniquement (services actifs, horaires, guichets+counter_services) — jamais de données ; source `is_template` recommandée (403 documenté sinon ? non : LA LOI ne l'exige pas — toute agence clonable, is_template = filtre UI ; consigné).
- **Session borne** : `POST /agencies/:id/kiosk-access` (crée kiosk + credentials bcrypt affichés une fois + QR d'installation) ; `POST /kiosk/session` (credentials → JWT 12 h scope agency role AUTHENTICATED, colonnes session sur kiosks) ; `DELETE /kiosk/session/:kioskId` (révocation → session_revoked_at, JWT refusé ensuite malgré exp valide).
- `POST /agents/import` : CSV LA LOI (colonnes fixées, ≤500, UTF-8) — rapport `{created, skipped, errors[{line,field,code,message}]}`, transaction par ligne, mots de passe initiaux aléatoires (jamais dans la réponse — envoi = F6, consigné), E.164 validé.
- **Theming** : `GET/PATCH /banks/:id/theme` — `appliedColors` = correction de contraste ≥4.5:1 (algorithme WCAG relative luminance, ajustement de lightness documenté, testé sur cas limites) ; `GET .../logo-upload-url` : URL signée R2 (S3 presign) — LÀ OÙ R2 n'est pas configuré (dev), 503 code documenté, jamais de crash.
- **Droit à l'oubli** : `POST /data/purge-phone` † (branche `purgePhone` DB-008, BANK_ADMIN, idempotent LA LOI) ; `GET /data/retention-policy`.

## Critères d'acceptation
- [ ] `API-009: clone → config identique, zéro ticket/user copié (test complet)`
- [ ] `API-009: session borne — login credentials → JWT 12h utilisable sur /tickets et heartbeat ; révocation → 401 immédiat (tests)`
- [ ] `API-009: import CSV — 500 OK, 501 → 422 ; lignes invalides → errors précis, valides créées ; téléphone non-E.164 → INVALID_PHONE_FORMAT`
- [ ] `API-009: theming — couleur faible contraste → appliedColors corrigé ≥4.5:1 mesuré ; presign R2 formé (test avec stub S3 local) ; sans config → 503`
- [ ] `API-009: purge-phone → {purged:true,affectedTickets} puis {purged:false} + entrée audit DATA_PURGE (test bout-en-bout)`
- [ ] `API-009: Schemathesis PASS (admin complet + public session/heartbeat côté 011)`

## Hors scope
UI onboarding (WEB-006) · envoi des credentials importés (F6) · upload R2 réel en prod (stub testé ; intégration réelle = déploiement) · supervision bornes lecture (API-011).
