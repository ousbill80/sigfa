# DB-009 : Corrections lot F2 (issues panel Boucle 3)

**Module** : F2 — Data · **Agent** : agent-database · **Dépend de** : DB-001..008 (DONE) · **Statut** : DONE (2026-07-11)
**Origine** : panel F2 (`18-panel-f2-synthese.md`). Aucun CRITICAL ; 2 MAJOR sécurité en tête.

## Exigences (EARS)
- **banks sous RLS (MAJOR)** : ENABLE+FORCE RLS sur `banks` + policy SELECT `id = nullif(current_setting('app.current_bank_id', true), '')::uuid` ; REVOKE INSERT/UPDATE/DELETE à sigfa_app sur banks (mutations = rôle plateforme) ; sans contexte → zéro ligne ; ajouter banks + `retention_policies` à BUSINESS_TABLES du scan exhaustif (26 entrées).
- **Mots de passe de rôles (MAJOR)** : plus aucun mot de passe en dur dans `0001_rls.sql`/`roles.sql` — paramétrage par variables d'env à l'exécution de la migration (défauts DEV uniquement via .env.example, doc de rotation prod) ; le harness de test injecte les siens.
- Seed démo : mots de passe `crypto.randomBytes` affichés une fois + garde `NODE_ENV !== 'production'` ; hash bcrypt réel (cost 12).
- `PHONE_HASH_KEY` : même rigueur que la clé de chiffrement (64 hex exigés).
- `upsert-daily-stats.ts` : passer aux requêtes paramétrées (cohérence purge.ts) — l'injection devient structurellement impossible.
- `.down.sql` manquants : 0000/0001/0002 créés et testés (down complet 0007→0000 → base vide).
- `public_holidays` : définition Drizzle `src/schema/public-holidays.ts` (source de vérité unique) alignée sur la migration existante.
- Découpage ≤30 lignes : `upsertDailyStats`, `seedDemoTenant`, `purgeExpiredPhones` (+ purgePhone/purgeAiHistory/seedDefaultNotificationTemplates/runSeed en passant), comportement inchangé.
- Rejets consignés : casse AnomalyStatus (aligne LA LOI, conforme) ; renommages d'index AI (MINOR, churn de migration sans gain — consigné) ; AuditEntryRow snake_case (JSDoc de justification suffit).

## Critères d'acceptation
- [ ] `DB-009: sigfa_app sans contexte → zéro ligne sur banks ; contexte A → uniquement banque A ; INSERT/UPDATE/DELETE banks refusés (tests)`
- [ ] `DB-009: grep migrations/roles.sql → zéro mot de passe littéral hors défauts .env.example documentés (test)`
- [ ] `DB-009: seed démo — mdp aléatoires, garde production, hash bcrypt vérifiable (tests)`
- [ ] `DB-009: PHONE_HASH_KEY invalide → échec explicite (test)`
- [ ] `DB-009: upsert-daily-stats paramétré — zéro interpolation dans le SQL (test structurel + tests existants verts)`
- [ ] `DB-009: chaîne down 0007→0000 → zéro table restante (test)`
- [ ] `DB-009: public_holidays présent dans le schéma Drizzle et cohérent avec la base (test)`
- [ ] `DB-009: aucune fonction >30 lignes dans src/ (vérification comptée) ; 181+ tests verts`

## Hors scope
Renommages d'index (consigné) · rotation réelle des secrets prod (exploitation) · CONTRACT-011 (déjà DONE, statut PRD corrigé par l'orchestrateur).
